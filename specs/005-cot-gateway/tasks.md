---
description: "Task list for CoT Gateway (Feature 005) — generated by /speckit.tasks"
---

# Tasks: CoT Gateway

**Input**: Design documents from `/specs/005-cot-gateway/`
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, contracts/ ✅, quickstart.md ✅

**Tests**: Included. plan.md G1「Test-First」明示採 TDD：每個階段先寫 **contract → unit → integration** 三層測試並確認失敗，再實作。

**Organization**: 依 spec.md 三個 User Story 分階段（US1=P1 雷達單源、US2=P1 融合升級、US3=P2 斷線/TTL/優雅關閉），每 story 獨立可測、獨立交付。

## Format: `[ID] [P?] [Story] Description`

- **[P]**: 檔案互不重疊、無前置任務未完成者，可並行
- **[Story]**: `[US1]` / `[US2]` / `[US3]`；Setup / Foundational / Polish 階段無 Story 標籤
- 路徑一律對應 plan.md §Project Structure（hyphen 目錄 `services/cot-gateway/`、底線模組 `cot_gateway`）

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: 建立 service 骨架；對齊 `services/sentrycs-sim/` / `services/echoshield-sim/` 布局（G5）。

- [ ] T001 建立 service 目錄骨架 `services/cot-gateway/{src/cot_gateway,tests/{contract,integration,unit},scripts,config/certs}`（含各層 `__init__.py`）對齊 plan.md §Project Structure
- [ ] T002 撰寫 `services/cot-gateway/pyproject.toml`：setuptools、`[project.optional-dependencies] dev`、`[tool.ruff]`、`[tool.black]`、`[tool.pytest.ini_options] asyncio_mode=auto`；runtime 依賴 `aiohttp>=3.9`、`pydantic>=2.6`、`structlog>=24.1`、`pyyaml`、`cryptography`；dev 依賴 `pytest>=8.0`、`pytest-asyncio>=0.23`、`freezegun>=1.4`、`ruff>=0.4`、`black>=24.3`
- [ ] T003 [P] 建立 `services/cot-gateway/README.md`（安裝 / 啟動 / 測試三段；引用 quickstart.md）
- [ ] T004 [P] 建立 `services/cot-gateway/scripts/smoke.sh` 對齊 `services/sentrycs-sim/scripts/smoke.sh`（啟動 + `ncat` 旁觀 + SIGINT 關閉）
- [ ] T005 [P] 建立 `services/cot-gateway/config/gateway.yaml` 範例（四段 + logging；內容依 research.md §R8 / quickstart.md §2）
- [ ] T006 [P] 建立 `services/cot-gateway/config/certs/README.md`（p12 產生方式 + PEM 離線轉檔替代方案；引用 research.md §R2）並加入 `.gitignore` 忽略 `*.p12` / `*.key`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: 所有 User Story 都依賴的基礎設施：config / logging / 型別 / 測試 fixture。**未完成前不得進入 Phase 3+**。

### Tests (先寫，確認 FAIL)

- [ ] T007 [P] 撰寫 `services/cot-gateway/tests/unit/test_config_fail_fast.py`：非法 port / ttl_s<=0 / cert_file 不存在 / max_retries<1 / logging.level 非法 → `ValidationError`（data-model §8）
- [ ] T008 [P] 撰寫 `services/cot-gateway/tests/unit/test_logging.py`：structlog JSON 輸出含 FR-GW-026 event 清單（`track_first_seen` / `correlation_hit` / `source_switch` / `ttl_expired` / `tak_connected` / `tak_reconnect` / `queue_full_drop` / `sentrycs_poll_failed` / `echoshield_disconnected` / `tak_max_retries_exceeded` / `invalid_wire_fields`）key 齊全

### Implementation

- [ ] T009 [P] 實作 `services/cot-gateway/src/cot_gateway/models/track.py`：`TrackSource` Enum 與 `UnifiedTrack` dataclass（data-model §1、§2 完整欄位 + 不變性 assert）
- [ ] T010 [P] 實作 `services/cot-gateway/src/cot_gateway/logging.py`：structlog JSON configurator（`logging.level` / `logging.json` 可切）
- [ ] T011 實作 `services/cot-gateway/src/cot_gateway/config.py`：pydantic v2 `GatewayConfig`、`EchoshieldConfig`、`SentrycsConfig`、`CorrelatorConfig`、`TakServerConfig`、`LoggingConfig`；`extra="forbid"`；YAML loader + `${ENV_VAR}` 展開；fail-fast validator（T007 應轉 GREEN）
- [ ] T012 實作 `services/cot-gateway/src/cot_gateway/cli.py` + `__main__.py`：argparse（`--config`、`--verbose`）、載入 config、啟動 `loop.GatewayMain`
- [ ] T013 [P] 建立 `services/cot-gateway/tests/__init__.py` + `services/cot-gateway/tests/conftest.py`：共用 fixture `echoshield_stub`（asyncio TCP server 逐行 feed；支援 `drop_after_n`）、`sentrycs_stub`（`aiohttp.test_utils.TestServer` with `fail_rate`）、`tak_stub`（明文 TCP sink，紀錄 newline-delimited 行 `list[str]`）、`frozen_clock`（freezegun wrapper）、`sample_tracks`（依 data-model §2 產 3 種 source 樣本）

**Checkpoint**: Foundation ready — T007/T008 轉 GREEN；所有 Story 可並行開工。

---

## Phase 3: User Story 1 — 雷達單源端對端 (Priority: P1) 🎯 MVP

**Goal**: EchoShield Sim → Gateway → TAK Server，ATAK 看到灰色 `a-u-A-M-F-Q-r` 未識別航跡（uid=`ECHO-TRK-XXX`），位置貼近 wire JSON，10 Hz 穩定推送。

**Independent Test**: 僅啟動 EchoShield Sim + Gateway + TAK stub（關閉 Sentrycs）；驗證 `ECHO-TRK-001` CoT 以 newline 分隔抵達 TAK，`<point lat lon hae>` 貼近 JSON 輸入，type 恆為 `a-u-A-M-F-Q-r`，`stale - time = 11000 ms`。

### Contract Tests (先寫，確認 FAIL) — TDD Layer 1

- [ ] T014 [P] [US1] `services/cot-gateway/tests/contract/test_echodyne_wire.py`：對 `contracts/echodyne-wire.md` — 必填 10 欄、值域（lat/lon/alt_m）、`track_status ∈ {Active,Lost}`；非法列 → WARNING `invalid_wire_fields` 並 skip（不中止）
- [ ] T015 [P] [US1] `services/cot-gateway/tests/contract/test_cot_xml_schema.py`：對 `contracts/cot-xml.md` — `event/point/detail/remarks/track` 必備屬性、`time`/`start`/`stale` ISO 8601 毫秒 + `Z` 結尾；ECHOSHIELD 單源場景 type=`a-u-A-M-F-Q-r`、uid 前綴 `ECHO-`、`stale - time = 11000ms`（SC-GW-009、SC-GW-012 的 ECHO 單源類）
- [ ] T016 [P] [US1] `services/cot-gateway/tests/contract/test_tak_uplink.py`（僅 US1 相關切片）：對 `contracts/tak-uplink.md` — newline-delimited 分隔、`asyncio.open_connection(ssl=context)` 握手、使用 `gateway.p12` → SSLContext；以 `trustme` 產自簽憑證於 contract test 內完成 SSL 握手驗證

### Unit Tests (先寫，確認 FAIL) — TDD Layer 2

- [ ] T017 [P] [US1] `services/cot-gateway/tests/unit/test_haversine.py`：台北 ↔ 台北 50 m 邊界；赤道／跨經度；極區輸入；地球半徑 6,371,000 m 驗證（FR-GW-010）
- [ ] T018 [P] [US1] `services/cot-gateway/tests/unit/test_stale_policy.py`（US1 切片）：`track_status=Active` → `stale - time = 11_000 ms`（其他 11s 規則；freezegun 毫秒精度）
- [ ] T019 [P] [US1] `services/cot-gateway/tests/unit/test_cot_generator.py`（US1 切片）：`UnifiedTrack(source=ECHOSHIELD)` → XML 屬性順序、字元逸出、ISO 8601 毫秒、uid=`ECHO-{radar_track_id}`、type=`a-u-A-M-F-Q-r`、`<remarks>` 僅含 classification（無 Model/Status/Operator）

### Implementation for User Story 1

- [ ] T020 [P] [US1] 實作 `services/cot-gateway/src/cot_gateway/correlate/haversine.py`：依 research §R3 手寫 Haversine（T017 轉 GREEN）
- [ ] T021 [P] [US1] 實作 `services/cot-gateway/src/cot_gateway/cot/uid.py`：`uid_for(track)`（`ECHO-{radar_track_id}` / `SENTRYCS-{rf_track_id}` / `FUSED-{rf_track_id}`）；預留 source 切換 API（US2 用）
- [ ] T022 [US1] 實作 `services/cot-gateway/src/cot_gateway/cot/stale.py`：`compute_stale(track, now) -> datetime` 三段式（Lost=0 / NEUTRALIZED=30 / 其他=11；Lost 優先）（T018 轉 GREEN）
- [ ] T023 [US1] 實作 `services/cot-gateway/src/cot_gateway/cot/generator.py`：`CotGenerator.generate(track) -> str`；`xml.etree.ElementTree` 組裝；毫秒 ISO 8601 + `Z`；type 由 source 決定（ECHOSHIELD/SENTRYCS→`a-u-A-M-F-Q-r` 灰色、FUSED→`a-h-A-M-F-Q-r` 紅色；對齊 FR-GW-015、contracts/cot-xml.md §4、SC-GW-012 合規表）；依賴 T021+T022（T015、T019 US1 部分轉 GREEN）
- [ ] T024 [US1] 實作 `services/cot-gateway/src/cot_gateway/echoshield/adapter.py`：`EchodyneAdapter` asyncio TCP client；逐行 JSON 解析 + 欄位驗證 + 非法 WARNING skip；`track_queue.put`（阻塞）；無限重試 5 s；INFO `echoshield_connected` / `track_parsed` / WARNING `echoshield_disconnected` / `invalid_wire_fields`（T014 轉 GREEN）
- [ ] T025 [US1] 實作 `services/cot-gateway/src/cot_gateway/tak/ssl_context.py`：載入 `gateway.p12` → `cryptography` 解為 PEM → `SSLContext.load_cert_chain`；PoC `check_hostname=False` / `verify_mode=CERT_NONE`；支援 PEM 直接載入旁路（research §R2）
- [ ] T026 [US1] 實作 `services/cot-gateway/src/cot_gateway/tak/transmitter.py`：`TakTransmitter.run()`；`asyncio.open_connection(ssl=...)`；消費 `cot_queue`；寫入 `{xml}\n`；斷線重連骨架（US3 完成指數退避）（T016 轉 GREEN）
- [ ] T027 [US1] 實作 `services/cot-gateway/src/cot_gateway/correlate/correlator.py` (US1 切片)：`TrackCorrelator.correlate(track)` — ECHOSHIELD 入 `radar_tracks`；US1 階段無 RF 候選時原樣回傳（FR-GW-012 預備）；`update_ttl`/`get_all_active_tracks` 先建最小介面
- [ ] T028 [US1] 實作 `services/cot-gateway/src/cot_gateway/loop.py`（US1 骨架）：`GatewayMain` 組裝 3 coroutine（echodyne_adapter + process_loop + tak_sender；Sentrycs/TTL 在 US2/US3 加入）；`track_queue`(1000) / `cot_queue`(500, put_nowait + QueueFull 處理)；`seen_uids` 初探（INFO `track_first_seen`）；signal handler SIGINT/SIGTERM（US3 強化）

### Integration Tests (先寫 → 轉 GREEN) — TDD Layer 3

- [ ] T029 [US1] `services/cot-gateway/tests/integration/test_us1_radar_only_e2e.py`：`echoshield_stub` 推 3 筆 TRK-001（10 Hz 間隔）→ `tak_stub` 收到 3 筆 CoT XML；驗證 uid=`ECHO-TRK-001`、type=`a-u-A-M-F-Q-r`、`<point>` 貼近 JSON、`stale-time=11000ms`；p95 延遲 < 100 ms（SC-GW-001 採樣）

**Checkpoint**: US1 完全運作 — `python -m cot_gateway --config config/gateway.yaml` 可在 ATAK 看到灰色航跡（MVP 可交付）。

---

## Phase 4: User Story 2 — 雷達 + RF 融合與 source 切換 (Priority: P1)

**Goal**: Sentrycs 輪詢加入；50 m / 3 s 命中即融合為 `FUSED-DRN-XXX`（type=`a-h-A-M-F-Q-r`，敵對紅色）；source 切換以雙訊息（舊 uid stale=time + 新 uid 首筆）交接；`<remarks>` 反映 DETECTED / MITIGATING / NEUTRALIZED；NEUTRALIZED 時 `stale-time=30_000ms`；禁一對多、多候選取最小。

**Independent Test**: EchoShield + Sentrycs 同時啟動且在 50 m / 3 s 內；觀察 TAK 收到：先 `ECHO-TRK-001` 幾筆灰 → 一筆 `ECHO-TRK-001 stale=now` → `FUSED-DRN-001` 紅；Sentrycs status 變 NEUTRALIZED 時 `stale-time=30000ms`。

### Contract Tests (先寫，確認 FAIL)

- [ ] T030 [P] [US2] `services/cot-gateway/tests/contract/test_sentrycs_poller.py`：對 `contracts/sentrycs-poller.md` — 1 Hz 輪詢、空陣列不清空 `rf_tracks`（保留至 TTL）、5xx / timeout → WARNING `sentrycs_poll_failed` 並 skip 當次（FR-GW-007）
- [ ] T031 [P] [US2] 擴充 `services/cot-gateway/tests/contract/test_cot_xml_schema.py`：新增 FUSED 與 SENTRYCS-only 場景 — type=`a-h-A-M-F-Q-r`、uid 前綴、`<remarks>` 含 Model/Status/Operator、NEUTRALIZED `stale-time=30000ms`（SC-GW-012 完整 8 類場景）

### Unit Tests (先寫，確認 FAIL)

- [ ] T032 [P] [US2] `services/cot-gateway/tests/unit/test_correlator_match.py`：50 m 邊界 / 3 s 邊界四象限；多候選取最小距離；禁一對多（FR-GW-010、US2-6）
- [ ] T033 [P] [US2] `services/cot-gateway/tests/unit/test_uid_source_switch.py`：ECHO→FUSED、SENTRYCS→FUSED、FUSED→ECHO、FUSED→SENTRYCS 四種切換的 `(old_uid, new_uid)` 正確性（research §R5 entity_key 規則）
- [ ] T034 [P] [US2] 擴充 `services/cot-gateway/tests/unit/test_stale_policy.py`：NEUTRALIZED → 30_000 ms；Lost 優先於 NEUTRALIZED
- [ ] T035 [P] [US2] 擴充 `services/cot-gateway/tests/unit/test_cot_generator.py`：`<remarks>` 組合（缺欄位省略 — 例：無 operator_lat 不出現該段）；FUSED Track 位置取雷達、model/operator/status 取 RF

### Implementation for User Story 2

- [ ] T036 [US2] 擴充 `services/cot-gateway/src/cot_gateway/correlate/correlator.py`：完整匹配演算法（data-model §3）— `rf_tracks` registry、`fused_tracks` registry、距離最小取 best、禁一對多（同一 rf 已被某 radar 佔用則跳過）；`build_fused(radar, rf)` 產 `UnifiedTrack(source=FUSED, correlation_id=f"FUSED-{rf.track_id}", 位置取雷達, model/operator/status 取 RF)`（T032 轉 GREEN）
- [ ] T037 [P] [US2] 實作 `services/cot-gateway/src/cot_gateway/sentrycs/adapter.py`：`SentrycsAdapter` `aiohttp.ClientSession` 1 Hz 輪詢 `GET /detections`；timeout=2 s；HTTP/JSON 錯誤 → WARNING `sentrycs_poll_failed` + skip；空陣列保留 rf_tracks；enqueue `track_queue`；INFO `sentrycs_poll`（T030 轉 GREEN）
- [ ] T038 [US2] 擴充 `services/cot-gateway/src/cot_gateway/cot/uid.py`：`detect_source_switch(track, prev_uid_by_entity_key) -> (old_uid|None, new_uid)`；實作 research §R5 entity_key（ECHOSHIELD=`radar:*`、SENTRYCS=`rf:*`、FUSED 同時檢兩 key）（T033 轉 GREEN）
- [ ] T039 [US2] 擴充 `services/cot-gateway/src/cot_gateway/loop.py`：加入 sentrycs_adapter 為第 4 coroutine；`process_loop` 處理 source 切換雙訊息（舊 uid 以當下座標 + `stale=time` enqueue 後，再 enqueue 新 uid CoT；`seen_uids.discard(old_uid)`）；INFO `source_switch` 事件；`correlation_hit/miss` INFO 事件
- [ ] T040 [US2] 擴充 `services/cot-gateway/src/cot_gateway/cot/generator.py`：完整 `<remarks>` 組合（classification / model / status / operator_lat/lon 缺欄位省略）（T035 轉 GREEN、T031 轉 GREEN）

### Integration Tests (先寫 → 轉 GREEN)

- [ ] T041 [US2] `services/cot-gateway/tests/integration/test_us2_fusion_upgrade.py`：echoshield_stub 推 TRK-001（固定座標）+ sentrycs_stub 於第 2 s 回 `DRN-001` (同座標) → tak_stub 先收 N 筆 ECHO-TRK-001 + 1 筆 ECHO-TRK-001 stale=now + FUSED-DRN-001（type=`a-h-A-M-F-Q-r`）；Sentrycs 更新 status=NEUTRALIZED → 下一筆 FUSED CoT `stale-time=30000ms` 且 `<remarks>` 含 `Status: NEUTRALIZED`
- [ ] T042 [US2] `services/cot-gateway/tests/integration/test_us2_no_cross_match.py`：echoshield_stub TRK-001 於 (25.04,121.56) + sentrycs_stub DRN-001 於 300 m 外 → 不融合，維持 ECHO-TRK-001 + SENTRYCS-DRN-001 各自獨立 uid；多雷達候選（TRK-001 / TRK-002 都在 50 m 內）→ 取距離最小融合，另一筆維持 ECHO

**Checkpoint**: US1 + US2 皆可獨立運作；融合升級、source 切換雙訊息、NEUTRALIZED 延長 stale 全部可驗。

---

## Phase 5: User Story 3 — 斷線重連 / TTL / 優雅關閉 (Priority: P2)

**Goal**: EchoShield 斷 → 5 s 後自重連、主程序不退出；TAK 斷 → 指數退避 1→2→4→8→16 封頂 60s，最多 5 次後 exit≠0；`cot_queue` 滿 drop-newest + WARNING；雷達 10 s 未更新 → TTL 推送 `stale=time` 最終 CoT；SIGINT/SIGTERM → ≤ 3 s 排空 + exit 0。

**Independent Test**: 依 quickstart.md §6 四情境（6.1 EchoShield 重連 / 6.2 TAK 退避 / 6.3 TTL 過期 / 6.4 SIGINT），日誌與 exit code 逐條驗證。

### Contract Tests (先寫，確認 FAIL)

- [ ] T043 [P] [US3] 擴充 `services/cot-gateway/tests/contract/test_tak_uplink.py`：指數退避序列 `1/2/4/8/16` 封頂 60 s；queue 滿 drop-newest + WARNING `queue_full_drop`；達 max_retries=5 → raise 讓 Gateway exit code≠0（FR-GW-021/022）

### Unit Tests (先寫，確認 FAIL)

- [ ] T044 [P] [US3] `services/cot-gateway/tests/unit/test_correlator_ttl.py`：`update_ttl(now)` — TTL 10 s 邊界（9.9 s 保留、10.1 s Lost）；標記 Lost 後從 registry 移除；回傳對應 uid list；雷達被移除不得影響 rf_tracks

### Implementation for User Story 3

- [ ] T045 [US3] 擴充 `services/cot-gateway/src/cot_gateway/correlate/correlator.py`：`update_ttl(now) -> list[str]` 完整實作（data-model §3）；track_status 轉 Lost；seen_uids 同步清掉交由 loop 處理（T044 轉 GREEN）
- [ ] T046 [US3] 擴充 `services/cot-gateway/src/cot_gateway/tak/transmitter.py`：指數退避 `backoff_initial_s → backoff_cap_s` 最多 `max_retries` 次；超過 raise `ConnectionError` + ERROR `tak_max_retries_exceeded`；`cot_queue.put_nowait` QueueFull → drop-newest + WARNING `queue_full_drop`；INFO `tak_connected` / `tak_reconnect`（T043 轉 GREEN）
- [ ] T047 [US3] 擴充 `services/cot-gateway/src/cot_gateway/loop.py`：新增 `ttl_loop`（1 Hz tick；呼叫 `update_ttl` → 對 Lost uid 產 `stale=time` 最終 CoT enqueue → `seen_uids.discard`）；SIGINT/SIGTERM handler 觸發 shutdown flow（停 adapter → 排空 cot_queue ≤ 3 s → 關 writer → return exit code 0）；頂層 `asyncio.gather(..., return_exceptions=True)`：TAK max_retries 異常冒泡為 exit code≠0，EchoShield/Sentrycs 異常不中止其他 coroutine（FR-GW-024）

### Integration Tests (先寫 → 轉 GREEN)

- [ ] T048 [P] [US3] `services/cot-gateway/tests/integration/test_us3_echoshield_reconnect.py`：echoshield_stub `drop_after_n=5` → Gateway WARNING `echoshield_disconnected` → 5 s 後自動重連 INFO `echoshield_connected`；期間 sentrycs_stub 輪詢與 tak_stub 傳輸不中斷；主 task 不退出
- [ ] T049 [P] [US3] `services/cot-gateway/tests/integration/test_us3_tak_exp_backoff.py`：tak_stub 拒絕連線 → 觀察 5 次 `tak_reconnect` 退避時距 `1/2/4/8/16` ±0.2s；期間 `queue_full_drop` WARNING；第 5 次失敗後主程序 exit code≠0；ERROR `tak_max_retries_exceeded`
- [ ] T050 [P] [US3] `services/cot-gateway/tests/integration/test_us3_ttl_expiry.py`：echoshield_stub 推 TRK-001 後 10 s 不再推；第 11 s tick → tak_stub 收到 `ECHO-TRK-001` `stale=time` 的最終 CoT；INFO `ttl_expired`；seen_uids 中移除
- [ ] T051 [P] [US3] `services/cot-gateway/tests/integration/test_graceful_shutdown.py`：正常運作中送 SIGINT → ≤ 3 s 內完成排空 + 關閉 + exit code 0；shutdown 期間不得接受新 track

**Checkpoint**: 三個 User Story 皆獨立可驗；PoC 完整路徑打通（含韌性）。

---

## Phase 6: Polish & Cross-Cutting Concerns

- [ ] T052 [P] 執行並完整通過 `services/cot-gateway/tests/contract/`、`tests/integration/`、`tests/unit/` 三層；`pytest --cov=cot_gateway` ≥ 80% 單元覆蓋率
- [ ] T053 [P] 效能量測：以 `tests/integration/test_us1_radar_only_e2e.py` 擴充 100 msg/s × 60 s 穩態跑；蒐集 p50/p95/p99 延遲；寫入 `specs/005-cot-gateway/perf-report.md`（驗證 SC-GW-001/002/003/011）
- [ ] T053b [P] 非法輸入魯棒性壓測 `services/cot-gateway/tests/integration/test_robustness_invalid_inputs.py`：以 `echoshield_stub` 連續注入 1,000 筆異常資料（混合：缺欄位、非 JSON 文本、`lat=999`、`track_status` 非 enum、UTF-8 截斷），期間交錯注入合法 Track；驗證 (a) Gateway 主程序全程不退出；(b) 1,000 筆異常皆有 WARNING/ERROR `invalid_wire_fields` 日誌且計數相符；(c) 異常 0 產生 CoT；(d) 壓測結束後合法 Track 仍能在 1 s 內送達 `tak_stub`（SC-GW-010 直接對應；補 tasks.md 先前覆蓋 gap）
- [ ] T054 [P] 補 `services/cot-gateway/README.md`：完整安裝 / 啟動 / 測試 / 故障排除（對齊 quickstart.md §10）
- [ ] T055 執行 `ruff check` + `black --check` 於整個 `services/cot-gateway/`；修正所有違規
- [ ] T056 依 `quickstart.md` §3–§6 於 `docker compose` 環境手動走一次完整驗證（US1 灰色 / US2 紅色切換 / US3 四情境）；截圖或錄 asciinema 附至 `specs/005-cot-gateway/validation.md`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 Setup**：無前置，可立即開始
- **Phase 2 Foundational**：依賴 Phase 1；**BLOCKS 所有 Story**
- **Phase 3–5 User Stories**：皆依賴 Phase 2 完成
  - US1 與 US2 皆 P1，US1 做為 MVP 先交付；US2 可在 US1 GREEN 後或並行（不同檔案）
  - US3（P2）於 US1/US2 穩定後再做；但 US3 的 unit test（T044）與 US1/US2 無耦合，可提前寫
- **Phase 6 Polish**：依賴所有選定 Story 完成

### User Story Dependencies

- **US1**：僅依賴 Foundational；為 MVP
- **US2**：共用 CoT generator / uid.py / transmitter / loop 的檔案會與 US1 merge；建議 US1 GREEN 後再改 `loop.py` / `generator.py`（T039/T040）以避免同檔衝突
- **US3**：共用 `loop.py` / `transmitter.py` / `correlator.py`；建議在 US2 GREEN 後再改（T046/T047）

### 單一 Story 內順序（TDD）

1. Contract tests（Layer 1）→ 確認 FAIL
2. Unit tests（Layer 2）→ 確認 FAIL
3. Implementation → 轉 GREEN
4. Integration tests（Layer 3）→ 補最後 E2E 驗證

---

## Parallel Opportunities

### Phase 1 Setup — 全部 [P] 並行（T001 除外為前置）

```
T003, T004, T005, T006 同時做（不同檔案）
```

### Phase 2 Foundational — Tests + Models 並行

```
T007, T008 並行寫測試；
T009, T010 並行實作 models/ + logging；
T013 並行寫 conftest；
T011 依賴 T007（validator），T012 依賴 T011
```

### Phase 3 US1 — Tests 全可並行，Implementation 部分並行

```
並行寫：T014, T015, T016, T017, T018, T019
並行實作：T020 (haversine), T021 (uid), T025 (ssl_context)
串行：T022 → T023 → T024/T026/T027 → T028 → T029
```

### Phase 4 US2 — Tests 全可並行，Adapter 與 Correlator 並行

```
並行寫：T030, T031, T032, T033, T034, T035
並行實作：T036 (correlator) 與 T037 (sentrycs adapter)
串行：T038 → T039 → T040 → T041/T042
```

### Phase 5 US3 — Integration tests 全部可並行

```
T048, T049, T050, T051 並行（不同 fixture / 不同子情境）
實作：T045 可並行 T046（不同檔）；T047 最後整合
```

### Phase 6 Polish

```
T052, T053, T053b, T054 並行
```

---

## Implementation Strategy

1. **MVP = Phase 1 + 2 + 3 (US1 only)**：交付「EchoShield → Gateway → TAK 灰色航跡」；此時 Gateway 可以真正跑起來給利害關係人看到 ATAK 上的第一個點。
2. **P1 完整** = 追加 Phase 4 (US2)：融合紅色目標 + source 切換，核心價值完成。
3. **P2 韌性** = 追加 Phase 5 (US3)：交付生產就緒的斷線 / TTL / 優雅關閉語意。
4. **Polish**：效能報告 + lint + 手動驗證。

每階段完成後，依 `.specify/extensions.yml` 的 `after_tasks` hook 可自動 commit。

---

## Task Summary

| 類別 | 數量 |
| --- | --- |
| Total tasks | **57** (T001–T056 + T053b) |
| Setup | 6 (T001–T006) |
| Foundational | 7 (T007–T013) |
| US1 (P1 MVP) | 16 (T014–T029) |
| US2 (P1 Fusion) | 13 (T030–T042) |
| US3 (P2 Resilience) | 9 (T043–T051) |
| Polish | 6 (T052–T056 + T053b) |
| Contract tests | 6 (T014, T015, T016, T030, T031, T043) |
| Unit tests | 10 (T007, T008, T017, T018, T019, T032, T033, T034, T035, T044) |
| Integration tests | 8 (T029, T041, T042, T048, T049, T050, T051, T053b) |
| 標記 [P] 可並行 | 34 |

### 獨立測試準則（每 Story）

- **US1**：`pytest tests/integration/test_us1_radar_only_e2e.py` 綠燈 + 手動 ATAK 看到灰色圖示（quickstart §4）
- **US2**：`pytest tests/integration/test_us2_fusion_upgrade.py tests/integration/test_us2_no_cross_match.py` 綠燈 + ATAK 看到灰→紅切換（quickstart §5）
- **US3**：`pytest tests/integration/test_us3_*.py tests/integration/test_graceful_shutdown.py` 綠燈 + quickstart §6 四情境手動驗證

### 建議 MVP 範圍

**Phase 1 + Phase 2 + Phase 3 (US1)** = 29 個任務 → 可獨立展示價值。

### Format Validation

✅ 所有 56 個任務皆符合 `- [ ] TXXX [P?] [USx?] 描述 + 路徑` 格式；Setup/Foundational/Polish 無 Story 標籤；每個任務包含明確檔案路徑。
